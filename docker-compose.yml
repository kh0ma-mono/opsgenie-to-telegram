services:
  opsgenie-to-telegram:
    build: .
    image: ${IMAGE:-opsgenie-to-telegram:latest}
    ports:
      - "8080:8080"
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      TELEGRAM_CHAT_ID: ${TELEGRAM_CHAT_ID}
    restart: unless-stopped
